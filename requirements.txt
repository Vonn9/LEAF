# LEAF Prompt Search Engine — Python dependencies
#
# Python: 3.12
#
# Versions pinned below are the ones used during development and the metrics
# reported in readme.md. Other recent minor versions of each library will
# almost certainly work; we recommend creating a fresh virtual environment
# and running `pip install -r requirements.txt` to match the environment
# exactly.
#
# After installation, run `python warmup.py` once to pre-download the
# HuggingFace model weights (~2.7 GB). See readme.md for the full setup flow.

# ── Core scientific stack ────────────────────────────────────────────────────
numpy>=2.1
pandas==2.2.3

# ── Learning-to-rank ────────────────────────────────────────────────────────
xgboost==2.1.3

# ── Embeddings & cross-encoder ──────────────────────────────────────────────
sentence-transformers==3.3.1

# ── Lexical retrieval & tokenization ────────────────────────────────────────
rank-bm25==0.2.2
nltk==3.9.1

# ── Vector database ─────────────────────────────────────────────────────────
chromadb==0.5.23
posthog==3.7.4  # chromadb 0.5.23 calls posthog.capture() with 3 positional args (old API); posthog>=4 breaks that

# ── HyDE local LLM client (Ollama runs separately) ──────────────────────────
ollama==0.4.4

# ── Hyperparameter optimization ─────────────────────────────────────────────
optuna==4.1.0

# ── Progress bars ───────────────────────────────────────────────────────────
tqdm==4.67.1

# ── EDA figures ─────────────────────────────────────────────────────────────
matplotlib==3.9.3

# ── Streamlit demo (streamlit_app.py) ───────────────────────────────────────
streamlit==1.41.1
